# Problem: Find Even or Odd Number

## Algorithm
- Start
- Input an integer `num` from the user
- Compute `remainder = num % 2`
- If `remainder` is equal to `0`:
  - Print "`num` is Even"
- Else:
  - Print "`num` is Odd"
- Stop
